local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "haskell", "python", "java", "markdown" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
